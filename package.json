{
  "name": "brandfetch-playground",
  "version": "1.0.0",
  "description": "Interactive playground for all 4 Brandfetch APIs — Logo, Brand, Search, and Transaction",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "vercel-dev": "vercel dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
